"""
Base Page - Base class for all Page Object element repositories.

Pages in this framework are pure selector containers (element repositories).
They define WHAT elements exist on each page, not HOW to interact with them.
All interaction logic lives in the BP (Business Process) layer.
"""


class BasePage:
    """Base class for all Page Object element repositories."""
    pass
